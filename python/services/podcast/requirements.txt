# Podcast microservice — use Python 3.12.x and a dedicated venv (see README).
# Python 3.13+ removed stdlib `audioop`; pydub needs this backport on those versions.
audioop-lts>=0.2.2; python_version >= "3.13"
flask==3.1.0
flask-cors==5.0.0
python-dotenv==1.0.1
google-generativeai==0.8.4
gTTS==2.5.4
pydub==0.25.1
